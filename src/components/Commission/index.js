import React, { PureComponent } from 'react'
import { SafeAreaView, Text } from 'react-native'

export default class CommissionScreen extends PureComponent {
  static propTypes = {

  }

  render() {
    return (
      <SafeAreaView>
        <Text>Commission Screen</Text>
      </SafeAreaView>
    )
  }
}
